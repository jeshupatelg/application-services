server:
  port: 8080

spring:
  application:
    name: portfolio-orchestrator

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true

portfolio:
  sandbox:
    url: ${PORTFOLIO_SANDBOX_URL}

