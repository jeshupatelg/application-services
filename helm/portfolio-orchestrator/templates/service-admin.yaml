{{- if .Values.ingress.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "portfolio-orchestrator.fullname" . }}-admin
  labels:
    {{- include "portfolio-orchestrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.adminPort | default 8080 }}
      targetPort: {{ .Values.service.adminTargetPort | default 8080 }}
      protocol: TCP
      name: admin
  selector:
    {{- include "portfolio-orchestrator.selectorLabels" . | nindent 4 }}
{{- end }}

