{{- if .Values.ingress.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "portfolio-orchestrator.fullname" . }}-display
  labels:
    {{- include "portfolio-orchestrator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.displayPort | default 80 }}
      targetPort: {{ .Values.service.displayTargetPort | default 80 }}
      protocol: TCP
      name: display
  selector:
    {{- include "portfolio-orchestrator.selectorLabels" . | nindent 4 }}
{{- end }}

